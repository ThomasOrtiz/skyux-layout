/**
 * Specifies how to display the flex children inside the flex component.
 */
export enum SkyFlexJustify {

  /**
   * Displays flex children as center-aligned.
   */
  center = 'center',

  /**
   * Displays flex children as left-aligned.
   */
  left = 'left'

}
